
<div class="container" id="main">
<div>&nbsp;</div>
<?php
$title = $context->localize($template['title']);
$this->headTitle($title);
?>

<!--Main layout-->
	
        <!--Third row-->
        <div class="row">
            <div class="col-md-12">
				<div class="card-block" style="background-color: #FFFFFF" id='calendar'></div>
			</div>
		</div>
	</div>
		
<script>

<?php
echo $this->partial('/partials/home-scripts');
?>

<?php foreach ($template['rows']['tabs']['content'] as $tabId => $tab) : ?>
	<?php if ($tab['level'] == 'community' || $tab['level'] == 'subject' && $subject_id) : ?>
		<?php if ($tab['type'] == 'calendar') : ?>
showCalendar('<?php echo $this->url($tab['route'], array('id' => $subject_id)) ?>', 'calendar');
		<?php endif;?>
	<?php endif;?>
<?php endforeach;?>

</script>
