	<div class="row jumbotron">

<?php foreach ($menu->specification as $entryId => $entryDef) : ?>

	<?php if ($entryDef['type'] == 'banner') : ?>

	    <div <?php if (array_key_exists('class', $entryDef)) echo 'class="'.$entryDef['class'].'"' ?>>
	        <div class="panel">
					<img width="100%" src="<?php echo $this->basePath($place->banner_src) ?>" />
			</div>
		</div>

	<?php elseif ($entryDef['type'] == 'menuEntry') : ?>

		<div class="col-md-2">
			<div align="center">
			<?php if ($context->isAllowed($entryDef['route'])) : ?>
				<button type="button" class="btn menu-btn <?php echo ($entryId == $pageId) ? 'btn-primary' : 'btn-default' ?>" style="width:100%; font-weight: bold; font-size: 110%" id="<?php echo $entryId ?>-anchor">
				<?php if (array_key_exists('glyphicon', $entryDef)) : ?>
					<span class="glyphicon <?php echo $entryDef['glyphicon'] ?>"></span>
				<?php endif;?>
						<?php echo $entryDef['label'][$context->getLocale()] ?>
				</button>
			<?php endif ?>
	
			</div>
		</div>

	<?php endif;?>
<?php endforeach;?>

	</div>

<script>

<?php foreach ($menu->specification as $entryId => $entryDef) : ?>
	<?php if ($entryDef['type'] == 'menuEntry') : ?>
$('#<?php echo $entryId ?>-anchor').click(function () { $(location).attr('href', '<?php echo $this->url($entryDef['route']) ?>'); });
	<?php endif;?>
<?php endforeach;?>

</script>