  <!--Header-->
  <div class="modal-header">

    <h4 class="modal-title font-weight-bold dark-grey-text" id="myModalLabel"><?php echo $context->localize($content['invoiceList']['title']) ?></h4>
    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
      <span aria-hidden="true">&times;</span>
    </button>
  </div>
  <div class="modal-body mx-3">

          <table class="table table-hover">
            <thead>
              <tr>
                <th>Libellé</th>
                <th>Montant (€)</th>
                <th></th>
              </tr>
            </thead>
            <tbody>

  <?php foreach ($commitments as $commitment) : ?>

              <tr>
                <th scope="row"><?php echo $commitment->caption ?></th>
                <td><?php echo $context->formatFloat($commitment->tax_inclusive, 2) ?></td>
                <td><a href="<?php echo $this->url('commitment/downloadInvoice', ['type' => $account->type, 'id' => $commitment->id]) ?>" target="_blank"><?php echo $context->localize($content['invoiceList']['navigation']['download']['labels']) ?></a></td>
              </tr>

  <?php endforeach;?>

            </tbody>
          </table>

        <!--Footer-->
        <div class="modal-footer">
          <button type="button" class="btn btn-grey btn-rounded btn-sm" data-dismiss="modal"><?php echo $context->localize($content['invoiceList']['navigation']['return']['labels']) ?></button>
        </div>
  </div>